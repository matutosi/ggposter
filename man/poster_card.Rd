% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/card.R
\name{poster_card}
\alias{poster_card}
\title{Build a section card: a rounded box with a header tab and content}
\usage{
poster_card(
  body,
  header = NULL,
  theme = poster_theme(),
  header_size = NULL,
  fit_content = FALSE,
  show_plot_area = FALSE
)
}
\arguments{
\item{body}{The content. Either a grob, or a ggplot object (converted with
\code{\link[ggplot2:ggplotGrob]{ggplot2::ggplotGrob()}}).}

\item{header}{Header label for the tab. \code{NULL}, \code{NA}, or \code{""} draws no header.}

\item{theme}{A \code{\link[=poster_theme]{poster_theme()}} object.}

\item{header_size}{Font size of the header, in points. Defaults to
\code{theme$base_size * 1.15}.}

\item{fit_content}{If \code{TRUE}, size the body cell to \code{body}'s own measured
height times \code{theme$content_pad_factor}, instead of filling however
much vertical space the card is given (the latter is what you want when
the card's height is a relative share of the column, e.g. from
\code{\link[=poster]{poster()}}'s numeric \code{section$height}; the former is what
\code{section$height = "auto"} uses).}

\item{show_plot_area}{If \code{TRUE}, draw a dashed border around the header
tab and around the body content, separately, on top of the content
without hiding it. When the body is a table/figure paired with a
bullet-list description (see \code{with_notes()} in \code{R/content.R}), the
main content and the notes column each get
their own border instead of one around the combined pair. Useful for
seeing exactly how much of a card each of its parts fills.}
}
\value{
A \link[gtable:gtable]{gtable::gtable} grob of class \code{poster_card}.
}
\description{
A \emph{card} is the repeating unit of a poster. It draws a rounded background box
(border in the theme accent), an optional header "tab" in the accent colour,
and a body grob underneath. Every content type (text, table, figure, image)
is wrapped in a card so the poster keeps one consistent look.
}
\details{
The returned object is a \link[gtable:gtable]{gtable::gtable} (a grob), which can be placed in
another gtable or drawn directly with \code{\link[grid:grid.draw]{grid::grid.draw()}}.
}
\examples{
body <- grid::textGrob("hello")
card <- poster_card(body, header = "OBJECTIVES", theme = poster_theme())
# grid::grid.newpage(); grid::grid.draw(card)
}
