% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_poster}
\alias{render_poster}
\title{Render a poster to PDF or PNG at true size}
\usage{
render_poster(x, file, scale = 1, dpi = 300)
}
\arguments{
\item{x}{A \code{ggposter} object, as returned by \code{\link[=poster]{poster()}}.}

\item{file}{Output file path. Extension determines the device (\code{.pdf} or
\code{.png}).}

\item{scale}{Shrink factor applied to the true paper size, e.g. \code{0.25} for
an A4-sized preview of an A1 poster. Font/line sizes stay proportional
because the whole canvas (not just DPI) is scaled.}

\item{dpi}{Resolution for PNG output. Ignored for PDF.}
}
\value{
The output file path, invisibly.
}
\description{
Saves a \code{\link[=poster]{poster()}} object at its real paper size (e.g. 594x841mm for A1).
The output format is chosen from \code{file}'s extension: \code{.pdf} uses
\link[grDevices:cairo]{grDevices::cairo_pdf} (vector output with CJK glyph embedding), anything
else uses \link[ragg:agg_png]{ragg::agg_png}.
}
\examples{
\dontrun{
p <- poster(spec)
render_poster(p, "poster.pdf")
render_poster(p, "preview.png", scale = 0.25)
}
}
