% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fonts.R
\name{poster_font}
\alias{poster_font}
\title{Find an available font family for a poster}
\usage{
poster_font(cjk = FALSE, preferred = NULL, fallback = "")
}
\arguments{
\item{cjk}{If \code{TRUE}, look for a CJK (Japanese) capable family.}

\item{preferred}{Character vector of family names to try, in order. If \code{NULL},
a built-in list of common families is used.}

\item{fallback}{Family name to return when none of \code{preferred} is installed.
Defaults to the empty string, which lets the graphics device pick.}
}
\value{
A single font family name (character scalar).
}
\description{
Chooses a font family that is actually installed on the system, so that
output renders (and embeds into PDF) without falling back to a substitute.
Latin text prefers clean sans-serif faces; CJK text prefers a Japanese
capable family.
}
\examples{
poster_font()
poster_font(cjk = TRUE)
}
